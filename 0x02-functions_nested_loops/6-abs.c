#include "main.h"
#include <stdlib.h>
/**
 * _abs - determines the absolute value of num.
 * @i: the number whose absolute value is being determined..
 *
 * Return: Returns the aboslute value.
 */
int _abs(int i)
{
	return (abs(i));
}
